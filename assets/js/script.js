var buttonEl = document.querySelector("#save-task");
console.log(buttonEl);

/* Targets the <button> element in HTML. The button element is now named buttonEl.
The used of camelCase marks the element as a JavaScript variable.
The El suffix indentifies this as a DOM element & helps to keep track of which variables store DOM elements*/
